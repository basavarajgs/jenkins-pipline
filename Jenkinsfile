pipeline {
 agent any ;
 stages {
   stage ('BUILD') {
     steps {
       echo "this is build stage"
     }
   }
 
 tage ('TEST') {
     steps {
       echo "this is build stage"
     }
   }

  tage ('DEPLOY') {
     steps {
       echo "this is build stage"
     }
   }
 }
}
